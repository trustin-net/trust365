name: ci-cd
run-name: Running CI/CD
on: [push]
env:
  sppkgPath: sharepoint/solution/trust-365.sppkg
  node-version: 16
jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 8
      - name: Use Node.js ${{ env.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.node-version }}
          cache: 'pnpm'
      - name: Install deps, test, build and package
        run: |
          pnpm install
          gulp test
          gulp bundle --ship
          gulp package-solution --ship
      - name: Archive sppkg
        uses: actions/upload-artifact@v3
        with:
          path: ${{ env.sppkgPath }}

  cd:
    needs: ci
    runs-on: windows-latest
    env:
      siteUrl: https://trustinnet.sharepoint.com/sites/intranet/
    steps:
      - name: Download sppkg
        uses: actions/download-artifact@v3
      - run: ls artifact

      - name: Deploy to app tenant
        id: PnPPowerShellDeploy
        uses: aakashbhardwaj619/action-pnp-powershell-deploy@v1.0.0
        with:
          SHAREPOINT_SITE_URL: ${{ env.siteUrl }}
          ADMIN_USERNAME: ${{ secrets.ADMINUSERNAME }}
          ADMIN_PASSWORD: ${{ secrets.ADMINPASSWORD }}
          APP_FILE_PATH: ${{ env.sppkgPath}}
          SKIP_FEATURE_DEPLOYMENT: true
          OVERWRITE: true
